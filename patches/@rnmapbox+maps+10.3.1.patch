diff --git a/node_modules/@rnmapbox/maps/android/src/main/java/com/rnmapbox/rnmbx/modules/RNMBXLocationModule.kt b/node_modules/@rnmapbox/maps/android/src/main/java/com/rnmapbox/rnmbx/modules/RNMBXLocationModule.kt
index 6af153e..77f15d1 100644
--- a/node_modules/@rnmapbox/maps/android/src/main/java/com/rnmapbox/rnmbx/modules/RNMBXLocationModule.kt
+++ b/node_modules/@rnmapbox/maps/android/src/main/java/com/rnmapbox/rnmbx/modules/RNMBXLocationModule.kt
@@ -1,6 +1,7 @@
 package com.rnmapbox.rnmbx.modules
 
 import com.facebook.react.bridge.*
+import com.facebook.react.modules.core.DeviceEventManagerModule
 import com.rnmapbox.rnmbx.location.LocationManager
 import com.facebook.react.module.annotations.ReactModule
 import com.rnmapbox.rnmbx.NativeRNMBXLocationModuleSpec
@@ -57,7 +58,16 @@ class RNMBXLocationModule(reactContext: ReactApplicationContext) :
             mLastLocation = location
             if (changed && (location != null) && shouldSendLocationEvent()) {
                 val locationEvent = LocationEvent(location)
-                emitOnLocationUpdate(locationEvent.toJSON())
+                // Old-arch compat: the codegen'd NativeRNMBXLocationModuleSpec.
+                // emitOnLocationUpdate wires events through a CxxCallbackImpl
+                // that is only initialized under TurboModules. On
+                // newArchEnabled=false the callback is null → NPE on first
+                // GPS update. Emit via the legacy RCTDeviceEventEmitter
+                // directly; the JS side subscribes through NativeEventEmitter
+                // at locationManager.ts:27, which reads from this same channel.
+                reactApplicationContext
+                    .getJSModule(DeviceEventManagerModule.RCTDeviceEventEmitter::class.java)
+                    ?.emit(LOCATION_UPDATE, locationEvent.toJSON())
             }
         }
     }
diff --git a/node_modules/@rnmapbox/maps/android/src/main/java/com/rnmapbox/rnmbx/utils/ViewTagResolver.kt b/node_modules/@rnmapbox/maps/android/src/main/java/com/rnmapbox/rnmbx/utils/ViewTagResolver.kt
index 0bedfdb..a089bb9 100644
--- a/node_modules/@rnmapbox/maps/android/src/main/java/com/rnmapbox/rnmbx/utils/ViewTagResolver.kt
+++ b/node_modules/@rnmapbox/maps/android/src/main/java/com/rnmapbox/rnmbx/utils/ViewTagResolver.kt
@@ -58,7 +58,8 @@ open class ViewTagResolver(val context: ReactApplicationContext) {
     }
 
     private val manager : UIManager
-        get() = UIManagerHelper.getUIManager(context, UIManagerType.FABRIC)!!
+        get() = UIManagerHelper.getUIManager(context, UIManagerType.FABRIC)
+            ?: UIManagerHelper.getUIManager(context, UIManagerType.DEFAULT)!!
 
     // calls on UiQueueThread with resolved view
     fun <V>withViewResolved(viewTag: Int, reject: Promise? = null, fn: (V) -> Unit) {
diff --git a/node_modules/@rnmapbox/maps/src/modules/location/locationManager.ts b/node_modules/@rnmapbox/maps/src/modules/location/locationManager.ts
index e9744c5..2f00855 100644
--- a/node_modules/@rnmapbox/maps/src/modules/location/locationManager.ts
+++ b/node_modules/@rnmapbox/maps/src/modules/location/locationManager.ts
@@ -22,10 +22,16 @@ const MapboxLocationManager: typeof NativeRNMBXLocationModule = Platform.select(
 const IsTurbo: boolean =
   typeof MapboxLocationManager.onLocationUpdate === 'function';
 
+// Old-arch compat: the JS-side IsTurbo detection above is unreliable —
+// codegen always defines onLocationUpdate on the spec regardless of
+// runtime arch, so this would steer us to the broken codegen
+// subscription path on newArchEnabled=false. Force the legacy
+// NativeEventEmitter path to pair with the native-side
+// RCTDeviceEventEmitter.emit fallback in RNMBXLocationModule.kt.
+// Remove this when @rnmapbox/maps either fixes the detection or
+// we move to newArchEnabled=true.
 export const LocationModuleEventEmitter =
-  Platform.OS === 'ios' || (Platform.OS === 'android' && !IsTurbo)
-    ? new NativeEventEmitter(MapboxLocationManager as any)
-    : null;
+  new NativeEventEmitter(MapboxLocationManager as any);
 
 /**
  * Location sent by locationManager
